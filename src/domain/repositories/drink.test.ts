import { DrinkRepository } from './drink-repository';

describe('DrinkRepository', () => {
  describe('getAvailableDrinksFor', () => {
    it('returns drinks ', () => {
      const repository = new DrinkRepository();
      // TODO: implement
    });
  });
});
